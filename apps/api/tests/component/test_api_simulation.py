"""Component test: POST /simulate end-to-end (TestClient + emulator + mocked LLM)."""

def test_simulate_endpoint_contract():
    """TODO(Claude Code): assert 200, response schema, auth required, household scoping."""
    assert True  # placeholder
